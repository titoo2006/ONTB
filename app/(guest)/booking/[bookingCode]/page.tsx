/**
 * SCREEN 5 — Booking Confirmation / Digital Ticket. PRD_Phase1.md §Screen 5.
 *
 * Booking code (large, monospace, never wrapping — DESIGN.md §2), QR, trip details,
 * headcount, PDF ticket, and the no-refund-on-no-show policy restated here.
 *
 * SECURITY.md §2 — a booking code alone must not expose a stranger's PII. Reaching
 * this page from a bare code needs a second factor (the booking email) before any
 * "view my ticket" flow is opened up. Resolve before this screen is built.
 *
 * SCAFFOLD STUB — not built yet.
 */
export default function BookingTicketPage() {
  return null;
}
