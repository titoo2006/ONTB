/**
 * SCREEN 9 — Admin Login. PRD_Phase1.md §Screen 9.
 * Same pattern as organizer login, checking `admin_users`. Non-admin → 404.
 * SECURITY.md §9 — admin sessions get a materially shorter idle timeout than
 * organizer sessions (organizers must not be logged out mid-shift).
 * SCAFFOLD STUB — not built yet.
 */
export default function AdminLoginPage() {
  return null;
}
