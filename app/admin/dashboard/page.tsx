/**
 * SCREEN 10 — Admin Dashboard (Overview). PRD_Phase1.md §Screen 10.
 * Bookings, guests, revenue with the owner/platform split shown separately,
 * check-in rate, no-show rate, per-trip table, aggregate nationality breakdown.
 * Read-only in Phase 1 — no write actions from this screen at all.
 * SCAFFOLD STUB — not built yet.
 */
export default function AdminDashboardPage() {
  return null;
}
