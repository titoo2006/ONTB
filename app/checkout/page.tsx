/**
 * SCREEN 3 — Checkout. PRD_Phase1.md §Screen 3.
 * Order summary + booker info form + "Pay $X".
 * The EGP disclosure sits next to the price at legible size (DESIGN.md §8).
 * SCAFFOLD STUB — not built yet.
 */
export default function CheckoutPage() {
  return null;
}
