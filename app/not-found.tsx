/**
 * Shared 404. Also the response for an authenticated-but-unauthorized visitor to
 * an organizer or admin route — Rule 10: we return 404, not 403, so a protected
 * route's existence isn't revealed.
 * SCAFFOLD STUB — not built yet.
 */
export default function NotFound() {
  return null;
}
