"use server";

/**
 * Screen 10 — admin dashboard reads. Read-only in Phase 1.
 *
 * Rule 10 — an authenticated user who is not in `admin_users` gets a 404, not a
 * 403. We do not confirm that the admin route exists to a caller who shouldn't
 * know it does.
 *
 * SCAFFOLD STUB.
 */

// TODO(scaffold): getDashboardAction().
export {};
