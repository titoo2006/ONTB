/**
 * Admin dashboard aggregation (Screen 10). Read-only in Phase 1 — no write actions
 * from the admin dashboard at all (PRD_Phase1.md, out of scope).
 *
 * SECURITY.md §8 — the nationality breakdown is AGGREGATE ONLY. It must not become
 * a guest-lookup-by-nationality tool.
 *
 * SCAFFOLD STUB.
 */

// TODO(scaffold): getDashboardSummary(), getTripBreakdown(), getNationalityCounts().
export {};
